# Supabase — Settings > API
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...        # service_role key (nunca expor no frontend)
SUPABASE_JWT_SECRET=seu-jwt-secret  # Settings > API > JWT Secret

# CORS — URL do frontend (Vercel). Separe múltiplas origens com vírgula.
FRONTEND_URL=https://seu-app.vercel.app

# Usado pelo start.sh em desenvolvimento
# PORT=8000
